
spring:
  datasource:
    username: ${POSTGRES_USERNAME}
    password: ${POSTGRES_PASSWORD}
    url: ${POSTGRES_URL}

  jpa:
    hibernate:
      ddl-auto: ${DB_DLL:create}

application:
  security:
    jwt:
      secret-key: ${AUTH_SECRET}

cors:
  allowed-origins: >
    https://vsproject-123-project.vercel.app